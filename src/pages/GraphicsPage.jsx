import { Header, NavBar } from "../ui/components"

export const GraphicsPage = () => {
  return(
    <section>
      <NavBar user="admin" modal={true}>
        Datos de Consuccion
      </NavBar>
    </section>
  )
}